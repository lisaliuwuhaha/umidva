// NOTE: 本地存储、弹窗等键名配置
export default {
  /** 示例：命名建议
   * 1. sessionStorage、localStorage 等以 storageKey-* 开头
   * 2. 弹窗名以 modalKey-* 开头
   * 3. url 固定后缀以 urlKey-* 开头（不一定不能用上）
   */
  storageKeyUserInfo: "storageKeyUserInfo",
  modalKeyLog: "modalKeyLog",
  urlKeyRecommendArticle: "recommend",
}
